# 練息場 JTE Platform — 協作原則(給 AI 與人)

## 第一原則:整體感,不硬整合(2026-07-08,使用者定案)

平台要的是**整體感**——同一個家(我的練息場)、同一種語氣與誠實邊界、工具間的轉介動線;
**不是硬整合**——不做系統間的量表對映、不強制翻譯彼此的語言、不為了統一而稀釋任何一方的公信力。
例:優勢幸福力(實證量表)× 八態能格(能量快照鏡)=並列呈現+雙向轉介,**不做對映表**。

## 恆守的北極星

- **鏡子非算命**:反映「是什麼」,不預測「將如何」;身分=格局,卦只是峰名。
- **誠實印章是品牌資產**:「v0.1 研究假設·待考」照掛;寧可少說、不可亂說。
- **作答不上傳**:資料在 localStorage;任何資料回流都是戰略決定(opt-in),不是功能。
- **C1(含平台版)**:單人內跨股、平台上跨工具,一律**並列不捏因果**;僅四對待可講關係,僅雷風=張力。
- **meta A 同源**:所有文案/定義進 canonical 資料檔(`octenso/bagua-data.js` 等),頁內不散寫,根治漂移。
- **AI=伴讀不是主筆**:正式報告由規則引擎組裝(可追溯、確定性);AI 負責讀後討論,守伴讀守則。

## 工程慣例

- 開發於指定分支,PR 合併進 main;GitHub Pages 自動部署。
- 測試:無 node;`python3 -m http.server` + headless chromium 讀 title 的 `RESULT pass=X fail=Y`;頁面不可有外部絕對網址 script。
- 視覺:`octenso/DESIGN-PRINCIPLES.md`(宣紙/墨/松綠、細線分段、不用 emoji)。
- 文件:設計決策進 `docs/superpowers/specs/`;總覽查 `octenso/COMPENDIUM.md`;文件網頁版在 `docs/octenso/`(入口 index.html)。
